import React from "react";

const VideoDetail = (props) => {
    console.log(props);
    return (

        <React.Fragment>
            Hello There
        </React.Fragment>

    );

}

export default VideoDetail;